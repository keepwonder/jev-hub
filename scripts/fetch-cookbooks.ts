/**
 * Cookbooks 抓取脚本（占位）
 *
 * 当前状态：Cookbooks 是手工翻译的，存放在 src/content/cookbooks/。
 * 待做：等 TypeSafe AI 公开 cookbook 列表/索引的稳定 URL 后，实现自动抓取。
 *
 * 这个 stub 保证 `npm run fetch:all` 不会因为缺失文件而中断。
 *
 * 退出码：0（成功但无操作），让 fetch:all 链式继续。
 */

console.log('[fetch-cookbooks] SKIPPED — 当前为手工维护，待接入官方源后启用。');
console.log('[fetch-cookbooks] 现有 20 个 cookbook 见 src/content/cookbooks/。');
process.exit(0);